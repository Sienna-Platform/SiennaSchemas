{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SupplementalAttributeAssociation",
  "description": "Links a supplemental attribute to the entity it describes. Two columns only: the type of either side is resolved through the entity registry rather than duplicated here, matching GridDB's `supplemental_attributes_association` table. Lives in Core because either side may be a Core or an Operations type.",
  "type": "object",
  "properties": {
    "attribute_id": {
      "description": "ID of the supplemental attribute.",
      "type": "integer"
    },
    "entity_id": {
      "description": "ID of the component the attribute describes.",
      "type": "integer"
    }
  },
  "required": [
    "attribute_id",
    "entity_id"
  ]
}
