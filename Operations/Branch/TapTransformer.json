{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "A 2-winding transformer, with a tap changer for variable turns ratio.\n\nThe model uses an equivalent circuit assuming the impedance is on the High Voltage Side of the transformer. The model allocates the iron losses and magnetizing susceptance to the primary side.",
  "properties": {
    "id": {
      "description": "Unique integer identifier for this component.",
      "type": "integer"
    },
    "name": {
      "description": "Name of the component. Components of the same type (e.g., `PowerLoad`) must have unique names, but components of different types (e.g., `PowerLoad` and `ACBus`) can have the same name.",
      "type": "string"
    },
    "available": {
      "description": "Indicator of whether the component is connected and online (`true`) or disconnected, offline, or down (`false`). Unavailable components are excluded during simulations.",
      "type": "boolean"
    },
    "active_power_flow": {
      "description": "Initial condition of active power flow through the transformer. Units: MW.",
      "type": "number",
      "x-unit": "MW"
    },
    "reactive_power_flow": {
      "description": "Initial condition of reactive power flow through the transformer. Units: MVAr.",
      "type": "number",
      "x-unit": "MVAr"
    },
    "arc": {
      "description": "An `Arc` defining this transformer `from` a bus `to` another bus.",
      "type": "integer"
    },
    "r": {
      "description": "Resistance. Per-unit on system base. Units: pu.",
      "type": "number",
      "x-unit": "pu"
    },
    "x": {
      "description": "Reactance. Per-unit on system base. Units: pu.",
      "type": "number",
      "x-unit": "pu"
    },
    "primary_shunt": {
      "description": "Primary shunt admittance. Per-unit on system base. Units: pu.",
      "$ref": "../../Core/common.json#/definitions/ComplexNumber",
      "x-unit": "pu"
    },
    "tap": {
      "description": "Normalized tap changer position for voltage control, varying between 0 and 2, with 1 centered at the nominal voltage. Units: 1.",
      "type": "number",
      "x-unit": "1"
    },
    "rating": {
      "description": "Thermal rating. Flow through the transformer must be between -`rating`. Units: MVA.",
      "type": "number",
      "x-unit": "MVA"
    },
    "base_power": {
      "description": "Base power for per unitization. Units: MVA.",
      "type": "number",
      "x-unit": "MVA"
    },
    "base_voltage_primary": {
      "description": "Primary base voltage. default of get_base_voltage(get_from(arc))) Units: kV.",
      "type": "number",
      "x-unit": "kV"
    },
    "base_voltage_secondary": {
      "description": "Secondary base voltage. default of get_base_voltage(get_to(arc))) Units: kV.",
      "type": "number",
      "x-unit": "kV"
    },
    "rating_b": {
      "description": "Second current rating. Units: MVA.",
      "type": "number",
      "x-unit": "MVA"
    },
    "rating_c": {
      "description": "Third current rating. Units: MVA.",
      "type": "number",
      "x-unit": "MVA"
    },
    "winding_group_number": {
      "description": "Vector group number ('clock number') indicating fixed phase shift between the `from` and `to` buses due to the connection group configuration.",
      "default": "UNDEFINED",
      "$ref": "../../Core/common.json#/definitions/WindingGroupNumber"
    },
    "control_objective": {
      "description": "Control objective for the tap changer for power flow calculations.",
      "default": "UNDEFINED",
      "$ref": "../../Core/common.json#/definitions/TransformerControlObjective"
    },
    "tap_limits": {
      "description": "Minimum and maximum tap ratio (per-unit turns ratio) over which the tap changer may regulate. Units: 1.",
      "default": {
        "min": 0.9,
        "max": 1.1
      },
      "$ref": "../../Core/common.json#/definitions/MinMax",
      "x-unit": "1"
    },
    "number_of_tap_positions": {
      "description": "Number of discrete tap positions spanning `tap_limits` (NTP in PSS/e); used to snap the tap to a realizable step.",
      "default": 33,
      "type": "integer"
    },
    "regulated_bus_number": {
      "description": "ACBus `number` whose voltage the tap changer regulates; `0` identifies the `to` bus of this transformer's arc (local control).",
      "default": 0,
      "type": "integer"
    },
    "voltage_setpoint": {
      "description": "Voltage setpoint at the regulated bus. Per-unit on system base. Units: pu.",
      "default": 1.0,
      "type": "number",
      "x-unit": "pu"
    }
  },
  "required": [
    "id",
    "name",
    "available",
    "active_power_flow",
    "reactive_power_flow",
    "arc",
    "r",
    "x",
    "primary_shunt",
    "tap",
    "base_power"
  ],
  "title": "TapTransformer",
  "type": "object"
}
