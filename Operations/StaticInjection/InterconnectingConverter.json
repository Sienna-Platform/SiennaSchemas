{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "Interconnecting Power Converter (IPC) for transforming power from an ACBus to a DCBus.",
  "properties": {
    "id": {
      "description": "Unique integer identifier for this component.",
      "type": "integer"
    },
    "name": {
      "description": "Name of the component. Components of the same type (e.g., `PowerLoad`) must have unique names, but components of different types (e.g., `PowerLoad` and `ACBus`) can have the same name.",
      "type": "string"
    },
    "available": {
      "description": "Indicator of whether the component is connected and online (`true`) or disconnected, offline, or down (`false`). Unavailable components are excluded during simulations.",
      "type": "boolean"
    },
    "bus": {
      "description": "ID of the bus on the AC side of this converter.",
      "type": "integer"
    },
    "dc_bus": {
      "description": "ID of the bus on the DC side of this converter.",
      "type": "integer"
    },
    "active_power": {
      "description": "Active power on the DC side. Units: MW.",
      "type": "number",
      "x-unit": "MW"
    },
    "rating": {
      "description": "Maximum output power rating of the converter. Units: MVA.",
      "type": "number",
      "x-unit": "MVA"
    },
    "active_power_limits": {
      "description": "Minimum and maximum stable active power levels. Units: MW.",
      "$ref": "../../Core/common.json#/definitions/MinMax",
      "x-unit": "MW"
    },
    "base_power": {
      "description": "Base power of the converter for per unitization. Units: MVA.",
      "type": "number",
      "x-unit": "MVA"
    },
    "reactive_power_limits": {
      "description": "Minimum and maximum reactive power limits. Set to `null` if not applicable. Units: MVAr.",
      "$ref": "../../Core/common.json#/definitions/MinMax",
      "x-unit": "MVAr"
    },
    "dc_current": {
      "description": "DC current on the converter. Units: A.",
      "default": 0.0,
      "type": "number",
      "x-unit": "A"
    },
    "max_dc_current": {
      "description": "Maximum stable dc current limits. Units: A.",
      "default": 100000000.0,
      "type": "number",
      "x-unit": "A"
    },
    "loss_function": {
      "description": "Linear or quadratic loss function with respect to the converter current.",
      "$ref": "../../Core/common.json#/definitions/InputOutputCurve"
    },
    "dc_control": {
      "description": "DC-side control mode of the converter.",
      "default": "DC_VOLTAGE",
      "$ref": "../../Core/common.json#/definitions/VSCDCControlModes"
    },
    "ac_control": {
      "description": "AC-side control mode of the converter.",
      "default": "AC_REACTIVE_POWER",
      "$ref": "../../Core/common.json#/definitions/VSCACControlModes"
    },
    "dc_setpoint": {
      "description": "DC-voltage target (when `dc_control` regulates DC voltage) or active-power order (otherwise). Per-unit on system base. Units: pu.",
      "default": 0.0,
      "type": "number",
      "x-unit": "pu"
    },
    "ac_setpoint": {
      "description": "AC-voltage magnitude target (when `ac_control` regulates AC voltage). Per-unit on system base. Units: pu.",
      "default": 1.0,
      "type": "number",
      "x-unit": "pu"
    },
    "dc_voltage_droop": {
      "description": "DC-voltage droop gain relating DC voltage to converter active power as `V_dc = dc_setpoint - dc_voltage_droop * P_c`. A value of 0.0 disables droop. Units: pu.",
      "default": 0.0,
      "type": "number",
      "x-unit": "pu"
    },
    "dynamic_injector": {
      "description": "ID of the corresponding dynamic injection device, if any.",
      "type": [
        "integer",
        "null"
      ]
    }
  },
  "required": [
    "id",
    "name",
    "available",
    "bus",
    "dc_bus",
    "active_power",
    "rating",
    "base_power",
    "active_power_limits"
  ],
  "title": "InterconnectingConverter",
  "type": "object"
}
