{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "An energy storage device, modeled as a generic energy reservoir.\n\nThis is suitable for modeling storage charging and discharging with average efficiency losses, ignoring the physical dynamics of the storage unit. A variety of energy storage types and chemistries can be modeled with this approach. For pumped hydro storage, alternatively see `HydroPumpTurbine` and `HydroReservoir`.",
  "properties": {
    "id": {
      "description": "Unique integer identifier for this component.",
      "type": "integer"
    },
    "name": {
      "description": "Name of the component. Components of the same type (e.g., `PowerLoad`) must have unique names, but components of different types (e.g., `PowerLoad` and `ACBus`) can have the same name.",
      "type": "string"
    },
    "available": {
      "description": "Indicator of whether the component is connected and online (`true`) or disconnected, offline, or down (`false`). Unavailable components are excluded during simulations.",
      "type": "boolean"
    },
    "bus": {
      "description": "ID of the bus that this component is connected to.",
      "type": "integer"
    },
    "prime_mover_type": {
      "description": "Prime mover technology according to EIA 923.",
      "$ref": "../../Core/common.json#/definitions/PrimeMovers"
    },
    "storage_technology_type": {
      "description": "Storage Technology Complementary to EIA 923.",
      "$ref": "../../Core/common.json#/definitions/StorageTech"
    },
    "storage_capacity": {
      "description": "Maximum storage capacity (can be in units of, e.g., MWh for batteries or liters for hydrogen). When in MWh, this value divided by base_power gives an approximate duration in hours, assuming unity power factor.",
      "type": "number",
      "x-unit": "MWh"
    },
    "storage_level_limits": {
      "description": "Minimum and maximum allowable storage levels [0, 1], which can be used to model derates or other restrictions, such as state-of-charge restrictions on battery cycling.",
      "$ref": "../../Core/common.json#/definitions/MinMax"
    },
    "initial_storage_capacity_level": {
      "description": "Initial storage capacity level as a ratio [0, 1.0] of `storage_capacity`.",
      "type": "number"
    },
    "rating": {
      "description": "Maximum AC side output power rating of the unit. Not to be confused with base_power.",
      "type": "number",
      "x-unit": "MW"
    },
    "active_power": {
      "description": "Initial active power set point of the unit. For power flow, this is the steady state operating point of the system. For production cost modeling, this may or may not be used as the initial starting point for the solver, depending on the solver used.",
      "type": "number",
      "x-unit": "MW"
    },
    "input_active_power_limits": {
      "description": "Minimum and maximum limits on the input active power (i.e., charging).",
      "$ref": "../../Core/common.json#/definitions/MinMax",
      "x-unit": "MW"
    },
    "output_active_power_limits": {
      "description": "Minimum and maximum limits on the output active power (i.e., discharging).",
      "$ref": "../../Core/common.json#/definitions/MinMax",
      "x-unit": "MW"
    },
    "efficiency": {
      "description": "Average efficiency [0, 1] `in` (charging/filling) and `out` (discharging/consuming) of the storage system.",
      "$ref": "../../Core/common.json#/definitions/InOut"
    },
    "reactive_power": {
      "description": "Initial reactive power set point of the unit.",
      "type": "number",
      "x-unit": "MW"
    },
    "reactive_power_limits": {
      "description": "Minimum and maximum reactive power limits. Set to `null` if not applicable.",
      "$ref": "../../Core/common.json#/definitions/MinMax",
      "x-unit": "MW"
    },
    "base_power": {
      "description": "Base power of the unit for per unitization.",
      "type": "number",
      "x-unit": "MW"
    },
    "operation_cost": {
      "description": "Operating cost of storage.",
      "descriptor": "or MarketBidCost",
      "$ref": "../../Core/common.json#/definitions/StorageCost"
    },
    "conversion_factor": {
      "description": "Conversion factor of `storage_capacity` to MWh, if different than 1.0. For example, X MWh/liter hydrogen.",
      "default": 1.0,
      "type": "number"
    },
    "storage_target": {
      "description": "Storage target at the end of simulation as ratio of storage capacity.",
      "default": 0.0,
      "type": "number"
    },
    "cycle_limits": {
      "description": "Storage Maximum number of cycles per year.",
      "default": 10000,
      "type": "integer"
    },
    "dynamic_injector": {
      "description": "Corresponding dynamic injection device.",
      "type": null
    }
  },
  "required": [
    "id",
    "name",
    "available",
    "bus",
    "prime_mover_type",
    "storage_technology_type",
    "storage_capacity",
    "storage_level_limits",
    "initial_storage_capacity_level",
    "rating",
    "active_power",
    "input_active_power_limits",
    "output_active_power_limits",
    "efficiency",
    "reactive_power",
    "base_power",
    "operation_cost"
  ],
  "title": "EnergyReservoirStorage",
  "type": "object"
}
