{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CombinedCycleFractional",
  "description": "Represents combined cycle generation when each unit models a specific configuration and aggregate heat rate. Operation exclusion groups are recorded as PlantAssociation rows with role='exclusion'.",
  "type": "object",
  "properties": {
    "id": {
      "type": "integer"
    },
    "name": {
      "type": "string",
      "description": "Name of the combined cycle fractional plant"
    },
    "configuration": {
      "$ref": "../../Operations/common.json#/$defs/CombinedCycleConfiguration"
    }
  },
  "required": [
    "id",
    "name",
    "configuration"
  ]
}
