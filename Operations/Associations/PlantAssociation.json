{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PlantAssociation",
  "description": "Association between a power plant supplemental attribute and a generating unit, identifying the unit's group within the plant: shafts, penstocks, PCCs, or exclusion groups. group_index semantics depend on the parent plant's type.",
  "type": "object",
  "properties": {
    "plant_id": {
      "type": "integer",
      "description": "ID of the parent plant supplemental attribute"
    },
    "entity_id": {
      "type": "integer",
      "description": "ID of the generating unit (entity) participating in the plant"
    },
    "group_index": {
      "type": "integer",
      "description": "Group number within the plant (shaft, penstock, PCC, or exclusion group, depending on the parent plant's type)"
    }
  },
  "required": [
    "plant_id",
    "entity_id",
    "group_index"
  ]
}
